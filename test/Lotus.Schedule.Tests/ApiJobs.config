<?xml version="1.0" encoding="utf-8" ?>
<jobs>
  <job name="ApiJobForCreate" type="Lotus.Schedule.Tests.ApiJobForCreate,Lotus.Schedule.Tests">
    <!--switch表示开关，on为开，off为关，默认为关-->
    <switch>off</switch>
    <!--cron表达式-->
    <cron>0/10 * * * * ? </cron>
  </job>
  <job name="ApiJobForUpdate" type="Lotus.Schedule.Tests.ApiJobForUpdate,Lotus.Schedule.Tests">
    <!--switch表示开关，on为开，off为关，默认为关-->
    <switch>off</switch>
    <!--cron表达式-->
    <cron>0/10 * * * * ? </cron>
  </job>
</jobs>